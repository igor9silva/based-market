// main component
export { default as OrbitalFlux } from './OrbitalFlux';

// types for external use
export type { Color, EffectType, GameConfig, GameStats, OrbitalFluxProps, PerkConfig } from './types';

// constants for external configuration
export { CONFIG_LIMITS, DEFAULT_GAME_CONFIG, DEFAULT_PERK_CONFIG } from './constants';
